input r;
x = 0;
ind = 0;

while (x < 101) {
    y = 0;
    if (x == 0) {
        y = 10;
    };

    while (y < 101) {
        if ((3 * (x - 50) * (x - 50) + (y - 50) * (y - 50)) < (r * r)) {
            putm ind 1;
        };
        y = y + 1;
        ind = ind + 1;
    };
    x = x + 1;
};

draw;